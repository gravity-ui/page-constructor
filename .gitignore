# Settings
.idea
.DS_Store
.vscode

# Libs
node_modules

# Generated content
/build
/storybook-static
/server
/configure
/styles/**/*.css
/coverage
/widget
/schema

*.tgz
.env
.env.development
/test-results/
playwright-report*
/blob-report/
.cache*

# Agent host integration is local-only — skills are canonically stored in
# .agents/skills/. Each developer/agent creates its own symlink (or copy) into
# the host's expected path. See AGENTS.md > "Agent skills".
.claude/skills
.cursor/skills
.codex/skills

# Skill eval artifacts (workspaces are local-only test outputs)
.agents/skills/*-workspace/
.claude/skills/*-workspace/
