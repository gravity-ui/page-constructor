import { Meta } from '@storybook/blocks';

<Meta title="Blocks/Companies/Params"/>

## Companies params

`type: companies-block`

`title: string` — Title

`images: ImageDeviceProps` — Images for different screen sizes (see in common types)
