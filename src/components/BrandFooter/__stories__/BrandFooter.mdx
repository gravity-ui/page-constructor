import {Meta} from '@storybook/blocks';

import {StoryTemplate} from '../../../demo/StoryTemplate.mdx';
import * as BrandFooterStories from './BrandFooter.stories.tsx';

<Meta of={BrandFooterStories} />
<StoryTemplate>

## Dependencies

For a detailed usage guide of the BrandFooter component, see [BrandFooter Usage](https://github.com/gravity-ui/page-constructor/blob/main/memory-bank/usage/brandFooter.md).

## Parameters

`className?: string` — Optional CSS class name for styling customization

</StoryTemplate>
