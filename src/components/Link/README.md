`type: "link"`

`text: text` — Link text

`url?: url` — Link URL

`arrow?: bool` — Draw an arrow on the right

`textSize?: l | m | s` — Link size

- 'l' - 17px
- 'm' - 15px
- 's' - 13px

**Margins:**

`top: xs`
