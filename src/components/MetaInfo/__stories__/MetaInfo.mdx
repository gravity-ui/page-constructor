import {Meta} from '@storybook/blocks';

import {StoryTemplate} from '../../../demo/StoryTemplate.mdx';
import * as MetaInfoStories from './MetaInfo.stories.tsx';

<Meta of={MetaInfoStories} />
<StoryTemplate>

## Usage

For a detailed usage guide of the MetaInfo component, see [MetaInfo Usage](https://github.com/gravity-ui/page-constructor/blob/main/memory-bank/usage/metaInfo.md).

## Parameters

`items: string[]` — Array of metadata items to display (required)

`className?: string` — Optional CSS class name for the container

</StoryTemplate>
