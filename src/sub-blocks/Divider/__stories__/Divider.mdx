import {Meta} from '@storybook/blocks';

import {StoryTemplate} from '../../../demo/StoryTemplate.mdx';
import * as DividerStories from './Divider.stories.tsx';

<Meta of={DividerStories} />
<StoryTemplate>

## Usage

For a detailed usage guide of the Divider sub-block, see [Divider Usage](https://github.com/gravity-ui/page-constructor/blob/main/memory-bank/usage/divider.md).

## Parameters

`size?: DividerSize` — Divider height: '0', 'xxs', 'xs', 's', 'm', 'l', 'xl', 'xxl', 'xxxl' (default is 'm')

`border?: boolean` — Whether to show a horizontal line (default is false)

</StoryTemplate>
