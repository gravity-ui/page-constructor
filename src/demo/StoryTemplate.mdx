import {Title, Subtitle, Description, Primary, Controls, Stories, Meta, Markdown } from '@storybook/blocks';

{/* you could pass template params if needed like <StoryTemplate foo="bar"/>*/}
export const StoryTemplate = () => (
    <>
        <Title />
        <Subtitle />
        <Primary />
        <Controls />
        <Stories />
    </>
);
