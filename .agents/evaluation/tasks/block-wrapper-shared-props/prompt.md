# Task: Explain the block wrapper that injects shared props

Explain how the common wrapper around blocks works — the layer that gives every block shared props such as `indent`, `anchor`, `visible`, `qa`, and `resetPaddings`. Describe the wrapper chain, where each shared prop is defined, where it is consumed, and how `indent` actually becomes spacing.
